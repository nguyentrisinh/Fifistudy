import {
    StyleSheet,
  } from 'react-native';

const styles = StyleSheet.create({
    container: {

    } ,
    titleGroup: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'center',
        marginLeft: 16,
        marginRight: 6
    },
    title: {
        fontWeight: 'bold'
    },
    moreGroup: {
        flexDirection: 'row',
        alignItems: 'center'
    }

});

export default styles;