import {
    StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
    image: {
        justifyContent: 'center',
        alignItems: 'center',
        borderRadius: 50,
    } 
});

export default styles;