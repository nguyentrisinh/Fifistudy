import {
    StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
    image: {
        justifyContent: 'center',
        alignItems: 'center',
        borderRadius: 8,
    } 
});

export default styles;